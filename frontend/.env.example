# Mock Authentication Mode
# Set to "true" to use mock auth service (no backend API calls, fake responses)
# This enables:
#   - Mock auth service (sign-in/sign-up work without backend)
#   - Simple sign-in/sign-out button in navigation
# Set to "false" to use real backend authentication API
VITE_MOCK_AUTH=true

# Backend API Configuration (only used when VITE_MOCK_AUTH=false)
VITE_BASE_API_URL=http://localhost:3200/api

# Base Path for Routing (used for GitHub Pages deployment)
# Set to the repository name for GitHub Pages: "/react-project-accelerator"
# Leave empty or set to "/" for local development or root domain deployment
VITE_BASE_PATH=
